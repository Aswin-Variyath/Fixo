import { ComponentFixture, TestBed } from '@angular/core/testing'; import { AvailabilitySchedule } from './availability-schedule'; describe('AvailabilitySchedule', () => { let component: AvailabilitySchedule; let fixture: ComponentFixture<AvailabilitySchedule>; beforeEach(async () => { await TestBed.configureTestingModule({ imports: [AvailabilitySchedule] }).compileComponents(); fixture = TestBed.createComponent(AvailabilitySchedule); component = fixture.componentInstance; await fixture.whenStable(); }); it('should create', () => expect(component).toBeTruthy()); });
