import { ComponentFixture, TestBed } from '@angular/core/testing'; import { provideRouter } from '@angular/router'; import { AvailabilityHeader } from './availability-header'; describe('AvailabilityHeader', () => { let component: AvailabilityHeader; let fixture: ComponentFixture<AvailabilityHeader>; beforeEach(async () => { await TestBed.configureTestingModule({ imports: [AvailabilityHeader], providers: [provideRouter([])] }).compileComponents(); fixture = TestBed.createComponent(AvailabilityHeader); component = fixture.componentInstance; await fixture.whenStable(); }); it('should create', () => expect(component).toBeTruthy()); });
