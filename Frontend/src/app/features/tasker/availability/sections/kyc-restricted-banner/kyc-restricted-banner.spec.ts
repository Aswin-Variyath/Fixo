import { ComponentFixture, TestBed } from '@angular/core/testing'; import { KycRestrictedBanner } from './kyc-restricted-banner'; describe('KycRestrictedBanner', () => { let component: KycRestrictedBanner; let fixture: ComponentFixture<KycRestrictedBanner>; beforeEach(async () => { await TestBed.configureTestingModule({ imports: [KycRestrictedBanner] }).compileComponents(); fixture = TestBed.createComponent(KycRestrictedBanner); component = fixture.componentInstance; await fixture.whenStable(); }); it('should create', () => expect(component).toBeTruthy()); });
