import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FindJobCard } from './find-job-card';
describe('FindJobCard', () => { let component: FindJobCard; let fixture: ComponentFixture<FindJobCard>; beforeEach(async () => { await TestBed.configureTestingModule({ imports: [FindJobCard] }).compileComponents(); fixture = TestBed.createComponent(FindJobCard); fixture.componentRef.setInput('job', { id: 'find-1', service: 'Electrical Wiring', customer: 'Rahul Nair', date: 'Aug 28, 2026', time: '10:00 AM', duration: '3 hrs', distance: '2.4 km away', location: 'Kozhikode, Kerala', amount: '₹1,200', description: 'Install electrical wiring for a residential property.' }); component = fixture.componentInstance; await fixture.whenStable(); }); it('should create', () => expect(component).toBeTruthy()); });
